pygame>=2.5.0
numpy>=1.24.0
pandas>=2.0.0
matplotlib>=3.7.0

# Performance optimization (10-50x speedup)
numba>=0.59.0  # JIT compilation for hot paths
jax>=0.4.34    # Optional: for GPU acceleration (CPU fallback works)
jaxlib>=0.4.34

# Note: Numba provides immediate 10-50x speedup on CPU
# JAX provides additional speedup but GPU support limited on Windows
# Both work together for maximum performance
